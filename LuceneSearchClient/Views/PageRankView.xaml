<Page x:Class="LuceneSearchClient.Views.PageRankView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.ignore.com"
      xmlns:converters="clr-namespace:LuceneSearchClient.Converters"
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d ignore" d:DesignHeight="500" d:DesignWidth="700"
        DataContext="{Binding PageRankViewModel, Source={StaticResource Locator}}">
    <Page.Resources>
        <converters:MatrixToDataViewConverter x:Key="MatrixToDataViewConverter" />
        <converters:ArrayToDataViewConverter x:Key="VectorToDataViewConverter"/>
        <converters:GenerateWebGraphConverter x:Key="GenerateWebGraphConverter"/>
        <converters:InitialVectorSetConverter x:Key="InitialVectorSetConverter"/>

    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto" MinHeight="146"/>
        </Grid.RowDefinitions>
        <GroupBox Header="WebGraph" Grid.Row="0">
            <Grid >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Content="Web Graph Excel File :"/>
                <TextBox Grid.Row="0" Grid.Column="1" Margin="5" IsEnabled="False" Text="{Binding WebGraphExcelFile}"  x:Name="WebGraphTextBox"/>
                <Button Content="Browse" Grid.Column="2" Grid.Row="0" Margin="5" Command="{Binding BrowseCommand}"/>
                <Button Content="Generate" Grid.Column="3" Grid.Row="0" Margin="5" Command="{Binding GenerateXmlsCommands}" IsEnabled="{Binding ElementName=WebGraphTextBox,Path=Text,Converter={StaticResource GenerateWebGraphConverter}}"/>
                <Label Content="Pages Xml File :" Grid.Row="1" Grid.Column="0" />
                <TextBox  Grid.Column="1" Margin="5" Grid.Row="1" IsEnabled="False" Grid.ColumnSpan="3" Text="{Binding PagesXmlFile}"/>
                <Label Content="Links Xml File :" Grid.Row="2" Grid.Column="0" />
                <TextBox x:Name="LinksXmlBox"  Grid.Column="1" Margin="5" Grid.Row="2" IsEnabled="False" Grid.ColumnSpan="3" Text="{Binding LinksXmlFile}"/>

            </Grid>
        </GroupBox>
        <GroupBox Header="Matrices" Margin="5" Grid.Row="1">
            <TabControl>
                <TabItem Header="Transition Matrix">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <DataGrid Grid.Row="0" Margin="5" Background="White" ItemsSource="{Binding TransitionMatrix ,Converter={StaticResource MatrixToDataViewConverter},Mode=TwoWay}" LoadingRow="OnLoadingRow" IsReadOnly="True"/>
                        <Button HorizontalAlignment="Right"  Content="Get Transition Matrix" Margin="2" Grid.Row="1"  Grid.Column="0" IsEnabled="{Binding ElementName=LinksXmlBox,Path=Text,Converter={StaticResource GenerateWebGraphConverter}}" Command="{Binding GetTransitionMatrixCommand}"/>
                    </Grid>
                </TabItem>
                <TabItem Header="Teleportation Matrix">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <DataGrid Grid.Row="0" Margin="5" Background="White" ItemsSource="{Binding TeleportationMatrix ,Converter={StaticResource MatrixToDataViewConverter},Mode=TwoWay}" LoadingRow="OnLoadingRow"/>
                        <Button Content="1/N" HorizontalAlignment="Right" Margin="2" Grid.Row="1"  Grid.Column="0"  IsEnabled="{Binding InitialPageRankVector,Converter={StaticResource InitialVectorSetConverter}}" Command="{Binding SetTeleportationCommand}"/>
                    </Grid>
                </TabItem>
            </TabControl>
        </GroupBox>
        <GroupBox Header="Initial PageRank Values " Margin="5" Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <Label Content="Initial Vector :" Grid.Row="0" Grid.Column="0"/>
                <DataGrid Grid.Row="0" Grid.Column="1"  Margin="5" Background="White" ItemsSource="{Binding InitialPageRankVector,Converter={StaticResource VectorToDataViewConverter},Mode=TwoWay}" />
                <Button Grid.Row="0" Content="1/N" Margin="2" Grid.Column="2" VerticalAlignment="Center" IsEnabled="{Binding InitialPageRankVector,Converter={StaticResource InitialVectorSetConverter}}" Command="{Binding SetInitialPageRankCommand}"/>

                <Label Content="Google PR :" Grid.Row="1" Grid.Column="0"/>
                <DataGrid Grid.Row="1" Grid.Column="1" Margin="5" Background="White" ItemsSource="{Binding PageRankVector,Converter={StaticResource VectorToDataViewConverter}}" />
                <Button Grid.Row="1" Content="Calculate" Margin="2" Grid.Column="2" VerticalAlignment="Center" IsEnabled="{Binding InitialPageRankVector,Converter={StaticResource InitialVectorSetConverter}}" Command="{Binding CalculatePageRankCommand}"/>

                <Label Content="Ameliorated PR :" Grid.Row="2" Grid.Column="0"/>
                <DataGrid Grid.Row="2" Grid.Column="1" Margin="5" Background="White" ItemsSource="{Binding AmelioratedPageRankVector,Converter={StaticResource VectorToDataViewConverter}}" />
                <Button Grid.Row="2" Content="Calculate" Margin="2" Grid.Column="2" VerticalAlignment="Center" IsEnabled="{Binding InitialPageRankVector,Converter={StaticResource InitialVectorSetConverter}}" Command="{Binding CalculateAmelioratedPageRankCommand}"/>

            </Grid>
        </GroupBox>        
    </Grid>
</Page>
