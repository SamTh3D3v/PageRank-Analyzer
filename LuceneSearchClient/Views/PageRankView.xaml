<Page x:Class="LuceneSearchClient.Views.PageRankView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.ignore.com"
      xmlns:converters="clr-namespace:LuceneSearchClient.Converters"
        mc:Ignorable="d ignore" d:DesignHeight="500" d:DesignWidth="700"
        DataContext="{Binding PageRankViewModel, Source={StaticResource Locator}}">
    <Page.Resources>
        <converters:MatrixToDataViewConverter x:Key="MatrixToDataViewConverter" />
        <converters:ArrayToDataViewConverter x:Key="ArrayToDataViewConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <GroupBox Header="WebGraph" Grid.Row="0">
            <Grid >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Content="Web Graph Excel File :"/>
                <TextBox Grid.Row="0" Grid.Column="1" Margin="5"/>
                <Button Content="Browse" Grid.Column="2" Grid.Row="0" Margin="5"/>
                <Button Content="Generate" Grid.Column="3" Grid.Row="0" Margin="5" IsEnabled="False"/>
                <Label Content="Pages Xml File :" Grid.Row="1" Grid.Column="0" />
                <TextBox  Grid.Column="1" Margin="5" Grid.Row="1" IsEnabled="False" Grid.ColumnSpan="3"/>               
                <Label Content="Links Xml File :" Grid.Row="2" Grid.Column="0" />
                <TextBox  Grid.Column="1" Margin="5" Grid.Row="2" IsEnabled="False" Grid.ColumnSpan="3"/>
                
            </Grid>
        </GroupBox>     
        <GroupBox Header="Matrices" Margin="5" Grid.Row="1">
            <TabControl>
                <TabItem Header="Transition Matrix">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <DataGrid Grid.Row="0" Margin="5" Background="White" />
                        <Button Content="Get Transition Matrix" Margin="2" Grid.Row="1"  Grid.Column="0" Command="{Binding GenerateMatrixCommand}"/>
                    </Grid>
                </TabItem>
                <TabItem Header="Teleportation Matrix">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <DataGrid Grid.Row="0" Margin="5" Background="White" />
                        <Button Content="1/N" Margin="2" Grid.Row="1"  Grid.Column="0" Command="{Binding GenerateMatrixCommand}"/>
                    </Grid>
                </TabItem>
            </TabControl>            
        </GroupBox>
        <GroupBox Header="Initial PageRank Values " Margin="5" Grid.Row="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <DataGrid Grid.Row="0" Margin="5" Background="White"/>
                <Button Content="1/N" Margin="2" Grid.Column="1" IsEnabled="False" Command="{Binding ResetInitialPageRankCommand}"/>

            </Grid>
        </GroupBox>
        <GroupBox Header="PageRank " Margin="10,10,0,0" Grid.Row="3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <DataGrid Grid.Row="0" Margin="5" Background="White" />
                <Button Content="Calculate Page Rank" Margin="2" Grid.Column="1" IsEnabled="False" Command="{Binding ResetInitialPageRankCommand}"/>

            </Grid>
        </GroupBox>
    </Grid>
</Page>
